{
  "name": "http-dumper",
  "version": "0.1.1",
  "description": "HTTP server that logs everything that gets sent to it.",
  "main": "src/index.js",
  "scripts": {
    "start": "./node_modules/.bin/nodemon src/index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "./node_modules/.bin/standard 'src/**/*.js'"
  },
  "author": "Johan Kanefur <johan@kanefur.se>",
  "license": "GPL-3.0",
  "dependencies": {
    "koa": "^2.13.1",
    "koa-bodyparser": "^4.3.0",
    "notera": "^0.2.2",
    "notera-transport-terminal": "^0.1.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.20",
    "standard": "^16.0.3"
  }
}
